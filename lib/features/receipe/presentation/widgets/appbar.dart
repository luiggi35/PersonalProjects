import 'package:flutter/material.dart';

class AppBarReceipe extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return (Text(
      "Cocktail Recipe",
    ));
  }
}
